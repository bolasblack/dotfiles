VERSION=$(curl -L https://yarnpkg.com/latest-version)

mkdir -p ~/.bin
curl -L -o ~/.bin/yarn https://github.com/yarnpkg/yarn/releases/download/v$VERSION/yarn-legacy-$VERSION.js
chmod u+x ~/.bin/yarn
